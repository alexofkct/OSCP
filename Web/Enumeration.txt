When the web service is persent,
A vulnerable version of CMS can be found. 
LFI and RFI can be found
RCE in the admin dashboard (using themes)
SQL injection can be found


To get a file through php by base64 encoding,
“php://filter/convert.base64-encode/resource=master.php” use this as the parameter

For example,
streamio.htb/admin/?debug=php://filter/convert.base64-encode/resource=master.php
